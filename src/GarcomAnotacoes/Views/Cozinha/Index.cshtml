@model IEnumerable<GarcomAnotacoes.Models.Cozinha>

<section class="container">
    <div class="row justify-content-center">
        <div class="col-8">
            <div class="card">
                <div class="card-header text-center text-white text-uppercase bg-primary">
                    <h3 class="m-0"> Cozinha  </h3>
                </div>
                <div class="card-body">
                    <table class="table table-striped table-bordered text-center">
                        <thead class="thead-dark">
                            <tr>
                                <th scope="col"> # </th>
                                <th scope="col"> Mesa </th>
                                <th scope="col"> Cozinha </th>
                                <th scope="col"> Nome dos Pratos </th>
                                <th scope="col"> Qtd de Pratos </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var cozinha in Model)
                            {
                                foreach (var pedido in cozinha.Pedidos)
                                {
                                    if (pedido.Ativo)
                                    {
                                        <tr>
                                            <td> @pedido.Id </td>
                                            <td> @pedido.NumeDaMesa </td>
                                            <td> @cozinha.Nome </td>
                                            <td> @pedido.NomeDoPrato</td>
                                            <td> @pedido.QuantidadeDePratos</td>
                                        </tr>
                                    }
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>